<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.fzq.utillib">

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name="com.fzq.utillib.crashutil.DefaultErrorActivity"
            android:process=":error_activity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:label="@string/error_title" />

        <provider
            android:name="com.fzq.utillib.crashutil.CaocInitProvider"
            android:authorities="com.fzq.utillib.crashutil.customactivityoncrashinitprovider"
            android:exported="false"
            android:initOrder="101" />

    </application>

</manifest>
